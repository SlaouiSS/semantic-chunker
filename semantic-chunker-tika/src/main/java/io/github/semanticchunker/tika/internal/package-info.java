/**
 * Internal mapping machinery for the Apache Tika adapter: the SAX content handler that folds Tika's
 * XHTML event stream into the core's document units, and its collaborators.
 *
 * <p>Nothing in this package is part of the module's public API. These types exist only to serve
 * {@link io.github.semanticchunker.tika.TikaDocumentExtractor} and may change without notice.
 */
package io.github.semanticchunker.tika.internal;

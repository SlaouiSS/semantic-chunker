/**
 * The Spring AI adapter: a {@link io.github.semanticchunker.model.ChunkingModel} implementation
 * that lets a language model be supplied by way of Spring AI (ARCHITECTURE.md, section 5).
 *
 * <p>This module depends on the core and satisfies one of the core's two abstractions; the core
 * does not depend on it. Everything specific to Spring AI is confined to this module.
 */
package io.github.semanticchunker.springai;
